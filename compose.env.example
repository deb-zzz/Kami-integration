# Copy to .env in repo root before running docker compose.
# Compose will substitute these variables in compose.yml.

# GHCR namespace/org/user that owns pushed images
GHCR_OWNER=your-github-org-or-user

# Default tag used by most services
GHCR_TAG=latest

# Optional separate tag for schema image
GHCR_SCHEMA_TAG=latest
